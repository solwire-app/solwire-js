import { type Product } from "@/types";
declare function validateSolwireSolTag(solTag: string): boolean;
declare function validateSolwireProducts(products: Product[]): boolean;
export { validateSolwireSolTag, validateSolwireProducts };
